# Optional overrides for pointing the frontend at a specific deployment
# Copy to .env.local (recommended) or .env and restart `npm run dev`

# Which Sui network the UI should use (must match wallet network)
VITE_NETWORK=testnet

# Published package ID that contains the Move modules (bunker, npc, etc)
# Example:
# VITE_PACKAGE_ID=0x...

# Shared object id created by `contracts::bunker::init` for `BunkerNpcLedger`
# VITE_BUNKER_NPC_LEDGER_ID=0x...

# Shared object id created by `contracts::raid::init` for `RaidHistory`
# VITE_RAID_HISTORY_ID=0x...

# Shared object id created by `contracts::marketplace::init` for `Marketplace`
# VITE_MARKETPLACE_ID=0x...
